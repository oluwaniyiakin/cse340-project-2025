<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/css/styles.css"> <!-- Updated CSS -->
</head>
<body>

    <header>
        <div class="logo"><h1>My Website</h1></div>
    </header>

    <div class="auth-container">
        <h2>Login</h2>
        
        <form action="/account/login" method="POST">
            <label for="loginEmail">Email:</label>
            <input type="email" id="loginEmail" name="account_email" required placeholder="Enter your email">
            
            <label for="loginPassword">Password:</label>
            <input type="password" id="loginPassword" name="account_password" required 
                   pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$">
            
            <button type="submit">Login</button>
        </form>

        <p>Don't have an account? <a href="/account/register">Register here</a></p>
    </div>

    <footer>
        <p>&copy; 2025 My Website</p>
    </footer>

</body>
</html>
