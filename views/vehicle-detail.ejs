<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <h1><%= title %></h1>
  </header>

  <main>
    <div class="vehicle-details">
      <img src="/images/<%= vehicle.image %>" alt="<%= title %>">
      <p><strong>Price:</strong> <%= formattedPrice %></p>
      <p><strong>Mileage:</strong> <%= formattedMileage %> miles</p>
      <p><strong>Fuel Type:</strong> <%= vehicle.fuel_type %></p>
      <p><strong>Transmission:</strong> <%= vehicle.transmission %></p>
    </div>

    <div class="vehicle-images">
      <% vehicle.gallery.forEach(image => { %>
        <img src="/images/<%= image %>" class="thumbnail" onclick="showImage('<%= image %>')">
      <% }) %>
    </div>
  </main>

  <script>
    function showImage(image) {
      document.querySelector('.vehicle-details img').src = '/images/' + image;
    }
  </script>
</body>
</html>
