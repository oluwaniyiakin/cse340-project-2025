<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= vehicle.inv_make %> <%= vehicle.inv_model %> - Details</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <h1>Vehicle Details</h1>
        <a href="/inventory">Back to Inventory</a>
    </header>

    <main class="vehicle-detail">
        <section class="vehicle-image">
            <img src="/images/vehicles/<%= vehicle.inv_image ? vehicle.inv_image : 'no-image.jpg' %>" 
                 alt="<%= vehicle.inv_make %> <%= vehicle.inv_model %>">
        </section>

        <section class="vehicle-info">
            <h2><%= vehicle.inv_make %> <%= vehicle.inv_model %> (<%= vehicle.inv_year %>)</h2>
            <p><strong>Price:</strong> $<%= vehicle.inv_price.toLocaleString() %></p>

            <h3>Vehicle Features</h3>
            <ul>
                <li><strong>Make:</strong> <%= vehicle.inv_make %></li>
                <li><strong>Model:</strong> <%= vehicle.inv_model %></li>
                <li><strong>Year:</strong> <%= vehicle.inv_year %></li>
                <li><strong>Price:</strong> $<%= vehicle.inv_price.toLocaleString() %></li>
            </ul>
        </section>
    </main>

    <footer>
        <p>&copy; <%= new Date().getFullYear() %> Vehicle Inventory</p>
    </footer>
</body>
</html>
