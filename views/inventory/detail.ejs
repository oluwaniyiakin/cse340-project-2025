<!DOCTYPE html>
<html lang="en">
<head>
<<<<<<< HEAD
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= vehicle.inv_make %> <%= vehicle.inv_model %> - <%= formattedPrice %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

  <!-- Optional header partial -->
  <%- include('../partials/header') %>
  <!-- Optional nav partial -->
  <%- include('../partials/nav') %>

  <main class="vehicle-detail-page">
    <!-- Vehicle Header -->
    <header class="vehicle-header">
      <h1><%= vehicle.inv_year %> <%= vehicle.inv_make %> <%= vehicle.inv_model %></h1>
      <p class="vehicle-price">No-Haggle Price: <strong><%= formattedPrice %></strong></p>
    </header>

    <!-- Main Detail Section -->
    <section class="vehicle-detail-container">
      <!-- Full-size Image -->
      <div class="vehicle-image">
        <img id="mainImage" 
             src="/images/vehicles/<%= vehicle.inv_image %>" 
             alt="<%= vehicle.inv_make %> <%= vehicle.inv_model %>">
      </div>

      <!-- Vehicle Information -->
      <div class="vehicle-info">
        <p><strong>Mileage:</strong> <%= formattedMileage %> miles</p>
        <p><strong>Description:</strong> <%= vehicle.inv_description %></p>
        <p><strong>Certification and Inspection:</strong> 
           The vehicle has been inspected by an ASE-certified technician, 
           indicating it meets certain quality standards.
        </p>

        <!-- Additional details if you have them in the DB -->
        <!-- Example placeholders below: -->
        <p><strong>Exterior Color:</strong> <%= vehicle.inv_color %></p>
        <p><strong>Fuel Type:</strong> Gasoline</p>
        <p><strong>Drivetrain:</strong> Front Wheel Drive</p>
        <p><strong>Transmission:</strong> Xtronic CVT</p>
        <p><strong>VIN:</strong> 311A87AP3KY3A2622</p>
        <p><strong>Stock #:</strong> 801C80</p>

        <!-- CTA Buttons -->
        <div class="cta-buttons">
          <button>Start My Purchase</button>
          <button>Contact Us</button>
          <button>Schedule Test Drive</button>
          <button>Apply for Financing</button>
        </div>
      </div>
    </section>

    <!-- Thumbnail Slider -->
    <section class="thumbnail-slider">
      <h3>Additional Images</h3>
      <div class="thumbnails">
        <!-- Primary Thumbnail -->
        <img class="thumbnail" 
             src="/images/vehicles/<%= vehicle.inv_thumbnail %>" 
             alt="Thumbnail" 
             onclick="updateMainImage('/images/vehicles/<%= vehicle.inv_image %>')">

        <!-- If there's an array of extra images in your DB, loop through them -->
        <% if (vehicle.extraImages) { %>
          <% vehicle.extraImages.forEach(function(img) { %>
            <img class="thumbnail" 
                 src="/images/vehicles/<%= img %>" 
                 alt="Thumbnail" 
                 onclick="updateMainImage('/images/vehicles/<%= img %>')">
          <% }) %>
        <% } %>
      </div>
    </section>

  </main>

  <!-- Optional footer partial -->
  <%- include('../partials/footer') %>

  <!-- Simple script to update main image -->
  <script>
    function updateMainImage(imgPath) {
      document.getElementById("mainImage").src = imgPath;
    }
  </script>
=======
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= vehicle.inv_make %> <%= vehicle.inv_model %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

    <div class="container">
        <div class="vehicle-header">
            <h1><%= vehicle.inv_year %> <%= vehicle.inv_make %> <%= vehicle.inv_model %></h1>
            <p class="price">No-Haggle Price: <strong>$<%= vehicle.inv_price %></strong></p>
        </div>

        <div class="vehicle-content">
            <div class="vehicle-image">
                <img id="mainImage" src="<%= vehicle.inv_image %>" alt="<%= vehicle.inv_make %> <%= vehicle.inv_model %>">
            </div>

            <div class="vehicle-info">
                <p><strong>Mileage:</strong> <%= vehicle.inv_miles %> miles</p>
                <p><strong>MPG:</strong> 29/37 (City/Highway)</p>
                <p><strong>Exterior Color:</strong> <%= vehicle.inv_color %></p>
                <p><strong>Fuel Type:</strong> Gasoline</p>
                <p><strong>Drivetrain:</strong> Front Wheel Drive</p>
                <p><strong>Transmission:</strong> Xtronic CVT</p>
                <p><strong>Stock #:</strong> 801C80</p>
                <p><strong>VIN:</strong> 311A87AP3KY3A2622</p>
            </div>
        </div>

        <div class="vehicle-thumbnails">
            <img class="thumbnail" src="<%= vehicle.inv_thumbnail %>" alt="Thumbnail">
            <img class="thumbnail" src="/images/car2.jpg" alt="Thumbnail">
            <img class="thumbnail" src="/images/car3.jpg" alt="Thumbnail">
        </div>

        <div class="buttons">
            <button>Start My Purchase</button>
            <button>Contact Us</button>
            <button>Schedule Test Drive</button>
            <button>Apply for Financing</button>
        </div>
    </div>

    <script>
        document.querySelectorAll(".thumbnail").forEach(img => {
            img.addEventListener("click", function() {
                document.getElementById("mainImage").src = this.src;
            });
        });
    </script>

>>>>>>> d9ce623bd073062dc418caa107ad7638d1eaa0c2
</body>
</html>
